# Runtime Backlog

## Now

- Keep the live Zulip bridge stable while validating the expanded runtime capabilities.
- Expand live smoke coverage so AgentSmith, Niaobe, and the Morpheus internal loop are all validated on the real Zulip runtime.

## Next

- Add stronger AgentSmith closeout/status flows now that Morpheus and the internal workers feed richer execution state back into the system.
- Add unblock/resume handling for blocked internal runs.
- Add a durable software-delivery artifact package for completed internal runs.

## Later

- Add multi-agent thread participation outside DM-only flow.
- Add richer browser/API integration beyond the current web research layer.
- Add deeper operational audit export/rotation if the runtime volume grows.

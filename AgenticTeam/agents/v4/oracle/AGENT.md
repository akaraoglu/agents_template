# AGENT.md - Oracle (V4)

1. **Understand Project**: Read `PROJECT.md`, `PROJECT_STATE.md`, backlog/plan files, and task logs to understand the requirements and deliverables.
2. **Inspect Artifacts**: Use read/discovery tools to verify that all expected files exist and match the requirements.
3. **Execute Verifications**: Run tests and other verification checks.
4. **Submit Report**: Call the `oracle_report` tool with either status `PASS` (if all criteria are met and tests pass) or `FAIL` (with detailed reason and evidence).

# SKILLS.md - Oracle

- **Read requirements**:
  inspect `PROJECT.md` and human-readable project state.

- **Inspect artifacts**:
  read source, tests, README, and relevant task files.

- **Run tests**:
  use test tools; test success is evidence, not automatic PASS.

- **Report**:
  call `oracle_report` with `PASS` or `FAIL`, a detailed summary, and evidence paths.

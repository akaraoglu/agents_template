# Tools - Oracle

Oracle is normally invoked by the Oracle runner. The available tools are:

```text
fs_read
fs_list
tests_discover
tests_run
oracle_report
```

Use no legacy runtime scripts and no outbound session tools.

# Task T001: Finalize requirements and project skeleton

## Objective

Make `PROJECT.md` specific, observable, and ready for implementation approval.

## Responsible AI

`project-lead-01` — leader role; default `qwen36-27b` profile.

## Context

Read `PROJECT.md`, `BRIEF.md`, `OPEN_QUESTIONS.md`, `management/TEST_GATES.md`, `management/TEST_GATES.toml`, `management/GIT_POLICY.md`, and `.codexteam/skills/project-init.md`.

## Scope

Clarify requirements, acceptance criteria, constraints, non-goals, verification, and delivery criteria.

## Allowed Paths

Project Markdown files and `management/` only.

## Required Outputs

Approved project specification, synchronized plan, resolved or explicitly recorded questions, concrete reproducible Development and Integration Gate commands, named architecture triggers, and milestone commit boundaries.

## Verification

Confirm every acceptance criterion is observable and maps to a task plus at least one configured test gate. Confirm the Integration Gate invokes the Development Gate before broader checks. Confirm the project is either an exact standalone Git root or explicitly opted out.

## Done Criteria

The operator approves the specification, `management/TEST_GATES.toml` is configured without placeholder commands, and architecture plus commit boundaries are explicit.

## Stop Conditions

Stop when a missing requirement would require guessing or expanding scope.

## Reporting

Return a draft specification summary for operator approval. Do not claim implementation readiness before approval.

# Task T002: Implement the approved thin slice

## Objective

Implement the smallest coherent behavior approved in `PROJECT.md`.

## Responsible AI

`developer-01` — developer role; default `qwen36-27b` profile.

## Context

Read `BRIEF.md`, this handoff, the approved thin-slice sections of `PROJECT.md` and `IMPLEMENTATION_PLAN.md`, applicable entries in `DECISIONS.md`, and `.codexteam/skills/implementation.md`. Inspect only the source and tests required by those named requirements; do not rediscover the whole repository.

## Scope

Only the approved MVP behavior.

## Allowed Paths

`src/`, task-relevant configuration, and implementation-focused tests.

## Required Outputs

Working implementation, focused evidence, and a reviewable draft. After Project Lead acceptance, emit one schema-valid final result.

## Verification

Run the smallest relevant implementation checks and record exact evidence.

## Done Criteria

Implementation outputs exist and are ready for independent verification.

## Stop Conditions

Stop if requirements are ambiguous, dependencies are unapproved, or a write would leave the project root.

## Reporting

Return a conversational draft first. Ordinary corrections remain in this responsible AI session and attempt.

# Task T003: Implement the approved thin slice

## Short Description

- Type: Feature
- Summary: Implement the smallest approved user-visible behavior with focused development tests.
- Outcome: A reviewable functional slice passes the Development Gate and is ready for independent testing.

## Objective

Implement the smallest coherent behavior approved in `PROJECT.md` and `ARCHITECTURE.md`.

## Responsible AI

- Owner: `developer-01`
- Role: `developer`
- AgentSpec: none
- Backend: `codex`
- Profile: `qwen38-27b`
- Reasoning: `medium`

## Context

Use only the accepted architecture decision and exact implementation and test
seams named below. This file is initializer scaffolding; the Project Lead must
replace scaffold targets before execution.

## Prior Discoveries

- Search status: cite the relevant control-root discovery note heading under
  `discoveries/` and its current-source verification target, or state: `No
  relevant prior discovery found.` Do not send the Developer to discover the
  control root.

## Context Targets

- Question: Which accepted behavior does this slice implement?
  Target: `PROJECT.md` — replace with the exact applicable `AC-*` rows.
  Use: Bound implementation and reporting to current project truth.
- Question: Which accepted design constrains the implementation?
  Target: `ARCHITECTURE.md` or `docs/decisions/` — replace with the exact heading.
  Use: Preserve component, API, data, and dependency boundaries.
- Question: Which source seam implements the behavior?
  Target: replace with at least one exact source path and symbol, or an exact new-file path.
  Use: Make the smallest coherent source change without broad discovery.
- Question: Which focused Developer-owned test proves the changed behavior?
  Target: replace with at least one exact test path and test name, or an exact new-test path.
  Use: Add or extend focused regression coverage before the Development Gate.
- Question: Which command defines development acceptance?
  Target: `management/TEST_GATES.toml` — `[development]`
  Use: Run focused checks that lead into the launcher-owned Development Gate.

## Scope

Only the approved MVP behavior within accepted architecture boundaries.

## Allowed Paths

`src/`, task-relevant configuration, and Developer-owned unit/algorithm and smoke tests. Do not modify architecture, integration expectations, fixtures, test data, or goldens.

## Task Write Scope

- `src/**`
- `tests/unit/**`
- `tests/smoke/**`
- `results/**`

## Context Mode

- `bounded-mcp`

## Execution Class

- `complex`

## Required Outputs

Working implementation, focused test evidence, architecture-conformance notes, and a reviewable draft. The launcher supplies Development Gate evidence.

## Verification

Identify the current Acceptance Criteria implemented by this task from `PROJECT.md`, run focused task checks, and report their `AC-*` references and exact evidence. The launcher runs the configured Development Gate after validating the draft. State that independent validation and integration acceptance remain unverified.

## Done Criteria

Implementation outputs exist, applicable criteria and development evidence are reported, the Development Gate passes, and the Test Engineer can run the Integration Gate without missing setup.

## Stop Conditions

Stop if requirements or architecture are ambiguous, dependencies are unapproved, or a write would leave the project root.

## Reporting

Write the launcher-assigned artifact report first. Name the applicable `AC-*` references, implementation outcome, and Development Gate evidence without claiming independent acceptance. Ordinary corrections remain in this session and attempt.

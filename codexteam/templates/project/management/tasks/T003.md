# Task T003: Implement the approved thin slice

## Objective

Implement the smallest coherent behavior approved in `PROJECT.md` and `ARCHITECTURE.md`.

## Responsible AI

`developer-01` - developer role; default `qwen36-27b` profile.

## Context

Read `BRIEF.md`, this handoff, the approved requirement and architecture sections, applicable ADRs, `management/TEST_GATES.toml`, `.codexteam/skills/implementation.md`, and `.codexteam/skills/development-testing.md`. Inspect only relevant source and Developer-owned tests.

## Scope

Only the approved MVP behavior within accepted architecture boundaries.

## Allowed Paths

`src/`, task-relevant configuration, and Developer-owned unit/algorithm and smoke tests. Do not modify architecture, integration expectations, fixtures, test data, or goldens.

## Required Outputs

Working implementation, focused tests, Development Gate evidence, architecture-conformance notes, and a reviewable draft.

## Verification

Run the configured Development Gate and record exact evidence. State that integration acceptance remains unverified.

## Done Criteria

Implementation outputs exist, the Development Gate passes, and the Test Engineer can run the Integration Gate without missing setup.

## Stop Conditions

Stop if requirements or architecture are ambiguous, dependencies are unapproved, or a write would leave the project root.

## Reporting

Return a conversational draft first. Ordinary corrections remain in this session and attempt.

# Architecture Decisions

Create an ADR only for a material decision with lasting architectural, compatibility, security, data, deployment, or dependency consequences.

Use filenames such as `ADR-0001-short-title.md`. Each ADR records status, context, decision, alternatives, consequences, and requirement references. The Project Lead accepts decisions; the Architect does not approve its own proposal.

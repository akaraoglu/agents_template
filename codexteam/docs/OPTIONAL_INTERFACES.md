# Optional Interfaces

HTTP, MCP, dashboards, remote APIs, and multi-user services are outside the current CodexTeam scope.

Any future interface must wrap the existing deterministic commands and contracts. It must not bypass workspace containment, result validation, independent verification, or leader-owned task closure.

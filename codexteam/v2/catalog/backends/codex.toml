schema_version = "2.0"
kind = "backend_definition"
definition_version = "1"
backend_id = "codex"
provider = "codex"
command = ["codex", "app-server"]
supported_operations = ["read", "write", "execute", "send"]
supported_resources = ["project_path", "mailbox", "evidence", "process"]
limitations = []

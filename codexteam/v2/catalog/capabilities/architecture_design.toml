schema_version = "2.0"
kind = "capability"
definition_version = "1"
capability_id = "architecture_design"
name = "Architecture design"
description = "Produce bounded architecture artifacts."
required_operations = ["read", "write", "send"]
required_resources = ["project_path", "mailbox"]

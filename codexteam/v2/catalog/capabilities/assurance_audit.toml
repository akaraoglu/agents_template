schema_version = "2.0"
kind = "capability"
definition_version = "1"
capability_id = "assurance_audit"
name = "Assurance audit"
description = "Audit a selected assurance domain and report evidence."
required_operations = ["read", "write", "send"]
required_resources = ["project_path", "evidence", "mailbox"]

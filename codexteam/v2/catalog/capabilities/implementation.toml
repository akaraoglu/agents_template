schema_version = "2.0"
kind = "capability"
definition_version = "1"
capability_id = "implementation"
name = "Implementation"
description = "Change scoped product and producer-test files."
required_operations = ["read", "write", "execute", "send"]
required_resources = ["project_path", "process", "mailbox"]

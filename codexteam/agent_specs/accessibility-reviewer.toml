schema_version = "1.0"
agent_spec_id = "accessibility-reviewer"
version = "1.0"
base_role = "reviewer"
description = "Accessibility-focused evidence review specialization."
capabilities = ["accessibility", "wcag", "assistive-technology"]
guidance_files = ["accessibility-review-specialization.md"]
permission_overlay = { allowed_change_patterns = [], denied_change_patterns = [], mcp_servers = [], mcp_tools = {}, allowed_evidence_types = [] }

# Changelog Memory

## Purpose
Record concise request and action history that future agents may need to understand recent repository changes.

## Entries
- Implemented CodexTeam Phase 23B as a real leader conversation agent. Added `leader_agent.py`, switched `talk_to_leader.py` to direct Ollama JSON mode by default, retained Codex provider as compatibility, and added tests for stored confirmations, model/tool failure handling, unsafe tool rejection, and full conversation flow.
- Implemented CodexTeam Phase 24 standalone read-only board command. Added `codexteam/src/codexteam/cli.py`, `codexteam/bin/codexteam board`, deterministic schema-versioned text/JSON output, and tests proving read-only/model-free behavior.
- Implemented CodexTeam Phase 25 project workspace and delivery layer. Added first-class project folders under `codexspace/projects`, team project records, safe project file read/write, delivery manifests, board project visibility, and leader project tools.
- Verified the real `gemma4:12b` leader path by creating `demo-team-20260615-171000`, creating/delivering project `demo-app-20260615-141149`, writing README and `src/main.py`, and executing the generated Python app. Fixed leader edge cases for preselected missing teams and plan approval application.
- Verified real leader project-management initialization by creating `pm-demo-team-20260615-171543` and project `project-management-demo-20260615-141625`. Added `initialize_project_management_docs` to avoid invalid long JSON content generation and produce `PROJECT.md`, `IMPLEMENTATION_PLAN.md`, `DECISIONS.md`, and `TASKS.md`.
- Updated CodexTeam project initialization to copy from `codexteam/templates/project_user/` or `CODEXTEAM_PROJECT_TEMPLATE` instead of hard-coded Markdown. Added a live leader smoke script and verified `gemma4:12b` created `template-demo-team-20260615-143101`, project `template-user-demo-20260615-143110`, and template docs.
- Revised the default CodexTeam project template to follow the old V4 test-project process surface from `run-e2e-fibonacci-v4-20260610-084233-1`: root state/report docs plus `management/PLAN.md`, `management/BACKLOG.md`, and `management/tasks/T001..T004.md`.
- Implemented CodexTeam Phase 26 real-life project E2E orchestration. Added `initiate_project`, `start_project_implementation`, `ProjectOrchestrator`, and `run_real_life_project_e2e.py`; verified generated project `cli-note-tool-20260616-055113` completed T001-T004, passed its tests, and delivered.
- Corrected Phase 26 terminology after user feedback: the controller-generated E2E is a scaffold, not real team implementation. Added `run_real_worker_project_e2e.py` to invoke local model workers.
- Verified the real-worker E2E with `ollama-files`: team `real-worker-cli-note-20260616-061838`, project `cli-note-tool-20260616-061838`, T001-T004 done, generated project tests `7 passed`, delivery completed. The `codex-exec` provider failed earlier with `unsupported call: exec_command1`.
- Implemented flexible project edit proposals. Added `ProjectEditProposal`, `ProjectEditor`, leader `propose_project_edit`, safety tests, and `run_project_edit_e2e.py`; verified editing `PROJECT.md` and `management/tasks/T002.md` after confirmation.
- Added CodexTeam project-local agent skills and task handoff contracts to the default project template. New projects now receive `AGENTS.md`, eight `.codexteam/skills/*.md` guidance files, and T001-T004 task files with explicit context, allowed paths, outputs, verification, evidence, and stop conditions.
- Added instruction-first document editing discipline. New projects now receive `document-editing.md` and `project-doc-map.md`, task handoffs reference them, and the leader prompt now tells agents to read docs first, preserve unrelated content, and avoid whole-document rewrites unless explicitly asked.
- Split CodexTeam model defaults. The conversational leader now defaults to `gemma4:26b` with a longer timeout, while worker/test defaults remain `gemma4:12b`. Added `leader_model` and `worker_model` config fields and updated core docs.
- Implemented a real project-sandbox leader runtime. Added `leader_runtime.py`, controller support, Phase 31 tests, and `run_real_leader_runtime_smoke.py`; verified a live Codex-backed run updated `BRIEF.md` and `CURRENT_TASK.md` inside a temporary project under `codexspace`.
- Wired `talk_to_leader.py` project-edit requests into the real project-sandbox leader runtime when an active project is attached, while keeping board/controller commands on the old control path. Fixed runtime project snapshots to compare raw file bytes so binary or non-UTF8 project files do not crash the leader, and verified a live front-CLI run updated `BRIEF.md` through the real runtime.
- Extended the real-runtime CLI path to carry recent conversation turns into the Codex project prompt, show the active project in the interactive prompt label, and return normal agent text instead of crashing when a runtime turn fails. Improved leader-runtime error reporting to include model errors, return code, stderr, and partial final message details; verified a live front-CLI run updated `BRIEF.md` with the conversation-context note.
- Unified CLI transcript handling across both leader paths. `talk_to_leader.py` now records user/assistant turns consistently whether a request goes through the real project runtime or the older controller-chat path, so recent context is no longer split by execution path.
- Narrowed the front-CLI router so natural project read requests like “show me the brief” go through the real project runtime instead of being blocked by broad `show`/`list` prefixes. Added a report-repair pass in the real runtime so non-mutating requests still succeed when the model answers in prose before JSON; verified a live `show me the brief` call completed through the runtime.
- Added safe runtime fallback in `talk_to_leader.py`. If the real project runtime fails for a project-scoped request, the CLI now falls back to the older controller-chat path before giving up, while preserving the unified transcript and storing the runtime error for debugging.

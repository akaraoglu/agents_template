# Task T003: Engineer and run the integration gate

## Objective

Verify the delivered source against every product acceptance criterion without trusting the developer result.

## Responsible AI

`integration-test-engineer-01` — Test Engineer using the wire-compatible `tester` role and runner-selected profile.

## Scope and Allowed Paths

Read product files, the accepted T002 result, and `management/TEST_GATES.md`. This controlled fixture already has complete acceptance coverage, so do not modify source, tests, README, or canonical state. Create only `results/t003-acceptance.txt`.

## Required Output

Record exact commands, exit statuses, and observations for the unittest suite, input 0, input 1, input 4, input 15, help, missing input, non-integer input, negative input, and input 16. Map observations to AC1–AC8.

## Task Write Scope

- `results/t003-acceptance.txt`

## Verification

Run the configured Integration Gate independently from the project root. Preserve any failure and classify it as product, test, environment, or unresolved; do not repair product code.

## Stop Conditions

Stop on the first required product failure and return the exact evidence to the Project Lead.

## Reporting

Return a conversational draft with command, observation, classification, artifact, and recommendation.

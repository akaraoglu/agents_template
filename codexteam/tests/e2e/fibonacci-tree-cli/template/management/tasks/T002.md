# Task T002: Implement the complete Fibonacci Tree CLI

## Objective

Implement the entire approved CLI, its standard-library tests, user README, and real development evidence.

## Responsible AI

`cli-developer-01` — developer role using the runner-selected profile.

## Scope and Allowed Paths

Create or modify only `src/fibonacci_tree_cli.py`, `tests/test_fibonacci_tree_cli.py`, `README.md`, and `results/t002-development.txt`. Do not create helper, audit, patch, or verification scripts.

## Required Output

- Complete recursive call-tree model using `F(0)=0`, `F(1)=1`.
- Deterministic Unicode rendering matching the approved `golden/fib-4.txt` output.
- CLI parsing, help, range validation, clean errors, and process statuses.
- Tests covering calculation, tree shape, exact rendering, help, valid input, and invalid input.
- README with reproducible invocation and representative output.
- `results/t002-development.txt` containing the exact unittest command and observed result.

## Verification

Run `python3 -B -m unittest discover -s tests -v` and direct CLI checks for 0, 1, 4, 15, help, and invalid input. Read `golden/fib-4.txt` before implementing the renderer.

## Stop Conditions

Stop for a contradictory product requirement, unavailable Python runtime, or need to write outside the allowed paths.

## Reporting

Return a conversational draft with exact evidence. Do not update canonical state or create `results/T002-att-001.json`.
